VIEW : user clicks addList btn

ctrl:add-list: display prompt - whats the list name

view: returns answer <myNewList>

ctrl:add-list: asks parent to add list with the name  <...>

parent: app-root: foreword request to service to add list

service: listService: sends a request to server to add list with name<..>

server: responds with new list || all lists

service: listService: receives respond with  new list || all lists ? from server

service: listService: foreword respond to parent

parent: updates lists
